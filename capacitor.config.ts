import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-ionic-pwa-template',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
